use std::io;

pub fn receive_input_command(input_test: &str) ->io::Result<()>{
    Ok(())

}

fn match_command(wd:path,input:Vec<&str>)->Vec<&str>{
    input
}

fn input_handling(return_result:Result<(), E>){
    println!(return_result)
}
