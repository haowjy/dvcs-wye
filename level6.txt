# acceptance 1
mkdir level6
cd level6
$[executable] init level6-remote
cd level6-remote
echo "Hello World" > a.txt
$[executable] add .
$[executable] commit "hello world"

cd ..
mkdir cwd
cd cwd
$[executable] clone ../level6-remote level6-remote
cd level6-remote

# PASS: the clone should have a have a hidden directory wtih files, and it works inside the clone
$[executable] log
$[executable] status




# acceptance 2
sudo rm -r level6
mkdir level6
cd level6
$[executable] init level6-remote
cd level6-remote
echo "Hello World" > a.txt
$[executable] add .
$[executable] commit "hello world"
echo "File B" > b.txt
$[executable] add .
$[executable] commit "b"

# PASS: the clone should have a have a hidden directory with files, and it works inside the clone
$[executable] log
$[executable] status

$[executable] checkout new branch SHA

# MAYBE: the clone copies the current head of the clone
cd ..
mkdir cwd
cd cwd
$[executable] clone ../level6-remote level6-remote

# PASS: the clone fails the second time
$[executable] clone ../level6-remote level6-remote

cd level6-remote
$[executable] heads